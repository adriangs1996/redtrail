name = "redtrail-probe"
description = "L2 Probing — differential probes per hypothesis, confirm or refute with evidence."
version = "1.0.0"

triggers = ["probe", "verify", "test", "differential", "l2", "confirm hypothesis"]

[deps]
commands = ["rt hypotheses list", "rt status"]
